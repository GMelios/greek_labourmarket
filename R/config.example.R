# config.example.R
# The path constant the README refers to. Copy this file to R/config.R in the
# same folder, then set DATA_DIR to your own local copy of the data. config.R
# is gitignored and never committed, so each person keeps their own path here
# and no laptop path or data ever reaches the repo.

DATA_DIR <- "/Users/you/data/Greece"
